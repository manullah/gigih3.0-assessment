# Installation

Project

```bash
# install packages
$ yarn install
```

# Running the app

```bash
# run the app
$ yarn dev

# run in http://localhost:5173 address, because socket.io just accpet origin from localhost
```
